package com.example.reply.ui.utils

enum class ReplyNavigationType {
    BOTTOM_NAVIGATION, RAIL_NAVIGATION, PERMANENT_NAVIGATION_DRAWER
}

enum class ReplyContentType {
    LIST_ONLY, LIST_AND_DETAIL
}